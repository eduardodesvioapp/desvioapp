version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "80:80"
    env_file:
      - .env.local

  ai-worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    env_file:
      - .env.local
    restart: always
